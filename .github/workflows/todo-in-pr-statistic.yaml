name: TODO in PR Statistic

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  statistic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: Aeliot-Tm/todo-registrar-statistic-action@bcac8b08d8e5c390df369769890557ed6504ba0f
        with:
          config: |
            paths:
              in: .
              append: bin/todo-registrar
              exclude:
                - tests/fixtures
                - var
                - vendor
            process:
              glueSameTickets: true
              glueSequentialComments: true
            registrar:
              type: DryRun
            tags:
              - todo
              - fixme
