name: TODO in PR Statistic

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  statistic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: Aeliot-Tm/todo-registrar-statistic-action@b40e5aabed01de895ebd234282696384aacf31c7
        with:
          config: |
            paths:
              in: .
              append: bin/todo-registrar
              exclude:
                - tests/fixtures
                - var
                - vendor
            process:
              glueSameTickets: true
              glueSequentialComments: true
            registrar:
              type: DryRun
            tags:
              - todo
              - fixme
